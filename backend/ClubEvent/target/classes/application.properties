spring.application.name=ClubEvent
server.port=8083
spring.datasource.url=jdbc:postgresql://localhost:5432/ClubEventPI
spring.datasource.username=postgres
spring.datasource.password=inception722
eureka.client.fetch-registry=false
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
spring.jpa.hibernate.ddl-auto=update
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dx4csxrbp}
cloudinary.api-key=${CLOUDINARY_API_KEY:342287811573954}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:MJAGFtxOk87mTCi1qrHohoZOGxE}

# ── Stripe ────────────────────────────────────────────
# INSERT your Stripe secret key here (starts with sk_test_ or sk_live_)
stripe.secret-key=${STRIPE_SECRET_KEY:sk_test_REPLACE_WITH_YOUR_STRIPE_SECRET_KEY}

# ── Mail (Gmail SMTP example) ─────────────────────────
# INSERT your Gmail address and an App Password (not your login password)
# Enable 2FA on Google then create an App Password at https://myaccount.google.com/apppasswords
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:REPLACE_WITH_YOUR_EMAIL@gmail.com}
spring.mail.password=${MAIL_PASSWORD:REPLACE_WITH_YOUR_GMAIL_APP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ── App ───────────────────────────────────────────────
app.frontend-url=${FRONTEND_URL:http://localhost:4200}

supabase.url=${SUPABASE_URL:REPLACE_WITH_YOUR_SUPABASE_URL}
supabase.service-role-key=${SUPABASE_SERVICE_ROLE_KEY:REPLACE_WITH_YOUR_SERVICE_ROLE_KEY}
supabase.bucket=${SUPABASE_BUCKET:ticket-passes}
